name: meal_planner
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.17.0
  rxdart: ^0.27.1
  provider: ^5.0.0
  quiver: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: any
  mockito: any

flutter:
  uses-material-design: true
